{
  "name": "service-portal",
  "version": "1.0.0",
  "private": true,
  "description": "A lightweight public portal for Yondavo products.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/dev-server.mjs",
    "test": "node --test tests/*.test.mjs"
  }
}
